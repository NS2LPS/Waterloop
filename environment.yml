name: nicegui-env
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - nicegui
      - plotly
      - pydantic
      - fastapi
      - uvicorn
      - requests
      - asyncua
      - numpy
      - mysql-connector-python
      - ydantic-settings 
      - python-dotenv
      